# Exclude source files (only deploy built output)
src/
public/
*.ts
*.tsx
!*.d.ts
tsconfig.json
tsconfig.*.json

# Exclude node_modules - standalone build includes only production deps
node_modules/

# Exclude development files
.env.local
.env*.local
.vscode/
.idea/
*.swp
*.swo

# Exclude build cache (keep only .next/standalone and .next/static)
.next/cache/
.next/trace
.next/routes-manifest.json
.next/build-manifest.json
.next/prerender-manifest.json
.next/images-manifest.json
.next/BUILD_ID
.next/package.json
.next/required-server-files.json
.next/server/
.next/server.js
.next/server.js.map

# Keep only standalone output and static files
# .next/standalone/ - contains bundled server
# .next/static/ - contains static assets

# Exclude other build outputs
out/
.turbopack/
build/

# Exclude test files
coverage/
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx
__tests__/
*.test.js
*.spec.js

# Exclude documentation (keep README)
*.md
!README.md

# Exclude git files
.git/
.gitignore

# Exclude CI/CD (Azure handles deployment)
.github/

# Exclude temporary files
*.log
*.tmp
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Exclude config files not needed in production
.eslintrc*
.prettierrc*
.editorconfig

